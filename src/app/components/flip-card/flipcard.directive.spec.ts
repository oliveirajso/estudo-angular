import { FlipcardDirective } from './flipcard.directive';

describe('FlipcardDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipcardDirective();
    expect(directive).toBeTruthy();
  });
});
